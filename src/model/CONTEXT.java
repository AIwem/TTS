package model;

public enum CONTEXT {
	
	TIME,
	LOCATION,
	MOOD,
	EMOTION,
	SPEED,
	DIRECTION;
	
//	public static CONTEXT parse(String cx){
//		return (CONTEXT)Enum.valueOf(typeof(CONTEXT),cx);
//	}
}
